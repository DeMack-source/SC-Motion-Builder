# SC-Motion-Builder
A One-Stop shop for filing legal motions in the state of Florida

## Local Development

This app loads `app-shell.html` at runtime, so use a local web server during development rather than opening `index.html` directly from disk. GitHub Pages is fine in production.
